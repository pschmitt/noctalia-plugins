{
  "settings": {
    "database_path": {
      "label": "Timewarrior database path",
      "description": "Directory containing Timewarrior's data files. Leave empty to use TIMEWARRIORDB or ~/.config/timewarrior."
    },
    "poll_interval": {
      "label": "Poll interval (seconds)",
      "description": "How often to refresh the tracked duration and week/month/year totals."
    },
    "overtime_hours": {
      "label": "Overtime threshold (hours)",
      "description": "Tracked hours today above which the widget switches to the overtime color."
    },
    "start_command": {
      "label": "Start command",
      "description": "Shell command run by the panel's start button. Leave empty to run `timew start`. Runs with TIMEWARRIORDB set, e.g. `~/bin/zhj tw::work-start`."
    },
    "stop_command": {
      "label": "Stop command",
      "description": "Shell command run by the panel's stop button. Leave empty to run `timew stop`."
    },
    "command_timeout": {
      "label": "Command timeout (seconds)",
      "description": "How long to wait for the start/stop command before giving up. Wrappers that sync or shut things down can take a while."
    },
    "panel_time_format": {
      "label": "Panel time format",
      "description": "Format of the panel's week/month/year/day totals. Placeholders: {hours}, {minutes}."
    },
    "bar_time_format": {
      "label": "Bar time format",
      "description": "Format of the tracked duration shown in the bar. Placeholders: {hours}, {minutes}."
    },
    "label_color": {
      "label": "Label color",
      "description": "Duration text color while under the overtime threshold."
    },
    "label_overtime_color": {
      "label": "Label overtime color",
      "description": "Duration text color once today's tracked time passes the overtime threshold."
    },
    "icon_size": {
      "label": "Icon size",
      "description": "Bar display size of the Timewarrior logo, in pixels."
    },
    "hide_when_inactive": {
      "label": "Hide when not tracking",
      "description": "Remove the widget from the bar while no time is being tracked. When off, it stays in place and shows a pause icon instead."
    }
  }
}
